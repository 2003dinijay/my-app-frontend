{
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    
    "prepare": "husky",
    "test": "echo \"Error: no test specified\" && exit 0"
    
    
  }
}
